import React from "react";
import styled from "styled-components";

import { Constants } from "../constants";

export const ContentBreak = styled.hr`
  background-color: ${Constants.Colors.theGroveGreen};
  margin: 0 0 2rem 0;
  padding: 0.25rem;
`;
